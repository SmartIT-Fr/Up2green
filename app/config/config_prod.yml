imports:
    - { resource: config.yml }

assetic:
    use_controller: true

propel:
    dbal:
        driver:               mysql
        user:                 username
        password:             "sm@rTp0wereD!pwd"
        dsn:                  mysql:host=localhost;dbname=up2green2;charset=UTF8
        options:              {}
        attributes:           {}

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug
