{% extends 'Up2greenEducationBundle::layout.html.twig' %}

{% block content %}
<div class="container">
{% for classroomPicture in classroomPictures %}
<div class="span3">
    <a href="#myModal-{{ classroomPicture.id }}" role="button" class="btn" data-toggle="modal" data-target="#myModal-{{ classroomPicture.id }}">
        <img class="classroom-picture img-polaroid" src="{{ asset(classroomPicture.picture) }}" />
    </a>
</div>
<div class="modal hide" id="myModal-{{ classroomPicture.id }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3>{{ classroomPicture.classroom.school.name }} {{ classroomPicture.classroom.name }}</h3>
  </div>
  <div class="modal-body">
    <p>
        <img class="classroom-picture img-polaroid" src="{{ asset(classroomPicture.picture) }}" />
    </p>
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
  </div>
</div>
{% endfor %}
</div>
{% endblock %}
