AuthUserFile /var/www/dev/up2green2/.htpasswd
AuthGroupFile /dev/null
AuthName "Secured site"
AuthType Basic

require valid-user

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.*)$ app.php [QSA,L]
</IfModule>
